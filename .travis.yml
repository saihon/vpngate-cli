language: go
go:
  - '1.12.x'
  # - tip
env:
  global:
    - GO111MODULE=on
sudo: false
branches:
  only: master
script:
  - make test
after_success:
  - GO111MODULE=off go get github.com/goreleaser/goreleaser && goreleaser release
  # - test -n "$TRAVIS_TAG" && GO111MODULE=off go get github.com/goreleaser/goreleaser && goreleaser release
  # - test "$TRAVIS_OS_NAME" = "linux" -a -n "$TRAVIS_TAG" && GO111MODULE=off go get github.com/goreleaser/goreleaser && goreleaser
  # - test -n "$TRAVIS_TAG" && curl -sL https://git.io/goreleaser | bash
